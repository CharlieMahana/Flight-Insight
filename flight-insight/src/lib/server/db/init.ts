// import clientPromise from '$lib/server/db';

// Place initialization for database here. This will be called once when the
// server starts.
// Look at https://github.com/CodoPixel/sveltemongo/blob/main/src/db/gfs.ts for
// image management using GridFS.
export default async function () {
	// const client = await clientPromise;
	// const db = client.db();
}